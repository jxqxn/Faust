.godot/
.import/
export.cfg
export_presets.cfg
addons/godot_ai/

# Godot regenerates these from the source .csv on import; keep only the .csv/.md.
docs/reference/*.csv.import
docs/reference/*.translation

# Test run log (regenerated by tools/run_gut.gd).
gut-test.log
gut-test.stderr.log

# Editor/tooling artifacts.
.zcode/
.reasonix/
nul

# Machine-specific MCP registration (Godot bridge + UI feedback bridge).
# Paths point at local tool installs; keep per-machine, not in the repo.
.mcp.json
tmp/
